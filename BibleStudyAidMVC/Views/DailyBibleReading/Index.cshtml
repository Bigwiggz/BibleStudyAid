@using BibleStudyAidMVC.ViewModels

@model IEnumerable<DailyBibleReadingVM>
@{
    ViewData["Title"]="Daily Bible Reading";
    Layout = "~/Views/Shared/AdminLTE/_Layout.cshtml";
}
<h1>ViewData["Title"]</h1>

@if (Model == null)
{
    <div id="loading">Loading...</div>
}
else
{
    <table class="table">
        <thead>
            <tr>
                <th>Lesson Learned</th>
                <th>Starting Scripture</th>
                <th>Ending Scripture</th>
            </tr>
        </thead>
        @foreach (var item in Model)
        {
            <tbody>
                <tr>
                    <td><a asp-controller="DailyBibleReading" asp-action="Details" asp-route-id="@item.Id" class="nav-link">@item.LessonLearnedDescription</a></td>
                    <td>@item.ScriptureStartPoint</td>
                    <td>@item.ScriptureEndPoint</td>
                </tr>
            </tbody>
        }

    </table>
}

<br />
<br />
<NavLink class="nav-link" href="dailybiblereading/add">
    <span class="oi oi-plus" aria-hidden="true"></span> Add Bible Reading Record
</NavLink>